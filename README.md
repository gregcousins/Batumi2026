# NeuroSAT 30-minute worksheet, revision 8

Keep these files together:

- `NeuroSAT_30min_Worksheet_v8.ipynb`
- `neurosat_lab.py`
- `toy_neurosat_checkpoint.npz`

Change only the parameter block in the main experiment cell. To render any
generated formula in standard mathematical notation, run:

```python
ns.display_cnf(formula)
```

Dependencies:

```bash
pip install numpy pandas
```
